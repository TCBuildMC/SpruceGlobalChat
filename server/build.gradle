dependencies {
    api project(":common")
    implementation "org.java-websocket:Java-WebSocket:1.5.6"
}

publishing {
    publications {
        create("MavenServer", MavenPublication) {
            from components.java
            artifactId = project.archives_base_name
        }
    }
}
